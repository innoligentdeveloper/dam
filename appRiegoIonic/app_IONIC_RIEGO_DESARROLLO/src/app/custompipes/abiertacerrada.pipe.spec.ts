import { AbiertacerradaPipe } from './abiertacerrada.pipe';

describe('AbiertacerradaPipe', () => {
  it('create an instance', () => {
    const pipe = new AbiertacerradaPipe();
    expect(pipe).toBeTruthy();
  });
});
